name: agent-harness-service-app

x-runtime-environment: &runtime-environment
  AGENT_HARNESS_PROFILE: service
  # runtime trace 写入本轮隔离的 /smoke bind mount，供根 smoke 在清理前导出。
  AGENT_HARNESS_OBSERVABILITY__PATH: /smoke/trace.jsonl
  AGENT_HARNESS_STORAGE__DSN_FILE: /run/secrets/agent_harness_storage_dsn
  AGENT_HARNESS_QUEUE__DSN: redis://redis:6379/0
  AGENT_HARNESS_BUDGET__FINGERPRINT_KEY_FILE: /run/secrets/agent_harness_budget_fingerprint_key
  SERVICE_APP_EXECUTOR_ID: agent-harness-service-worker
  SERVICE_APP_RECLAIM_IDLE_SECONDS: ${SERVICE_APP_RECLAIM_IDLE_SECONDS:-1}

x-runtime-service: &runtime-service
  build:
    context: .
    dockerfile: Dockerfile
  image: ${SERVICE_APP_IMAGE:-agent-harness-service-app:smoke}
  user: "10001:${SERVICE_APP_RUNTIME_GID:-10001}"
  profiles: ["service"]
  environment: *runtime-environment
  volumes:
    - ${SERVICE_APP_SMOKE_DIR:?SERVICE_APP_SMOKE_DIR is required}:/smoke
  secrets:
    - agent_harness_storage_dsn
    - agent_harness_budget_fingerprint_key

services:
  postgres:
    image: ${SERVICE_APP_POSTGRES_IMAGE:-postgres:18.4@sha256:3a82e1f56c8f0f5616a11103ac3d47e632c3938698946a7ad26da0df1334744a}
    profiles: ["service"]
    environment:
      POSTGRES_DB: agent_harness
      POSTGRES_USER: agent_harness
      POSTGRES_PASSWORD_FILE: /run/secrets/agent_harness_postgres_password
    secrets:
      - agent_harness_postgres_password
    volumes:
      - agent_harness_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent_harness -d agent_harness"]
      interval: 2s
      timeout: 2s
      retries: 30

  redis:
    image: ${SERVICE_APP_REDIS_IMAGE:-redis:7.2.14@sha256:f0707c78ea880b293ccdeb410c9c0a8ccae93fe7128799b751333a698b0a39a7}
    profiles: ["service"]
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 30

  migration:
    <<: *runtime-service
    command:
      - python
      - -m
      - app.migrate
      - --profile
      - service
      - --profiles-dir
      - /app/configs/profiles
    depends_on:
      postgres:
        condition: service_healthy

  api:
    <<: *runtime-service
    command:
      - agent-harness-service
      - serve
      - --profile
      - service
      - --profiles-dir
      - /app/configs/profiles
      - --host
      - 0.0.0.0
      - --port
      - "8000"
    ports:
      - "${SERVICE_APP_API_PORT:-58000}:8000"
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health', timeout=2)"
      interval: 2s
      timeout: 3s
      retries: 30

  worker:
    <<: *runtime-service
    command:
      - python
      - -m
      - app.workers.runtime_worker
      - --profile
      - service
      - --profiles-dir
      - /app/configs/profiles
      - --workspace-root
      - /smoke/workspace
      - --artifact-root
      - /smoke/artifacts
    environment:
      <<: *runtime-environment
      SERVICE_APP_READY_FILE: /tmp/runtime-worker-ready
      SERVICE_APP_SMOKE_RECOVERY_MARKER: /smoke/recovery-handler.json
      SERVICE_APP_SMOKE_RECEIPT_MARKER: /smoke/worker-b-receipt.json
      SERVICE_APP_SMOKE_RECLAIM_RELEASE: /smoke/reclaim-release
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "test", "-f", "/tmp/runtime-worker-ready"]
      interval: 2s
      timeout: 2s
      retries: 30

volumes:
  agent_harness_postgres_data:

secrets:
  agent_harness_storage_dsn:
    file: ${SERVICE_APP_STORAGE_DSN_FILE:?SERVICE_APP_STORAGE_DSN_FILE is required}
  agent_harness_budget_fingerprint_key:
    file: ${SERVICE_APP_BUDGET_FINGERPRINT_KEY_FILE:?SERVICE_APP_BUDGET_FINGERPRINT_KEY_FILE is required}
  agent_harness_postgres_password:
    file: ${SERVICE_APP_POSTGRES_PASSWORD_FILE:?SERVICE_APP_POSTGRES_PASSWORD_FILE is required}
