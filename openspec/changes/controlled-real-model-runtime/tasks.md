## 1. 公共契约与配置 TDD

- [ ] 1.1 冻结 `API-Contract.md` 的 typed deployment、route/error/attempt/budget charge 公共 seam，并在 `design.md` 固定 AC-077 至 AC-084 的精确 production/test/CI producer 映射；主 `docs/acceptance-matrix.md` 只在精确红灯节点真实存在后加入同一 `pending` 行，不得提前制造证据或勾选完成。
- [ ] 1.2 新增配置合并红灯合同，覆盖 profile/Agent YAML/`.env`/`_FILE`/direct env/overrides 优先级、canonical dynamic credential ref、`Foo`/`FOO` direct-file 与 alias 碰撞、空/非法 segment、provider ambient env 不进入 typed settings 和 fake 离线兼容，并保存预期失败证据。
- [ ] 1.3 新增 deployment schema、endpoint security 与 model catalog 红灯合同，覆盖完整字段、provider identity、typed `endpoint_policies` 的 ref/version/provider/origin/classifier/default-catalog exact match、unknown policy fail closed、可选 completion classifier 与 endpoint digest、启用 response retry 却缺 classifier 时 fail closed，以及 `model_catalogs` 的 canonical ref/version/provider/model/request-shape/input-bound strategy/envelope/cost-enabled/price-source/price/digest exact match。证明 deployment/Agent/request/provider 不得自证或覆盖模型输入上界与价格；cost enabled 时价格与 price-source identity 必须完整，cost disabled 时两项价格、price-source identity 与全部 cost bounds 必须同时为 null；prompt/output cap、token/cost 公式、secret 序列化与 traceback 脱敏、origin/credential binding、redirect/proxy 和 local literal-loopback 均 fail closed，且失败时 storage/queue/reservation/client/DNS/HTTP 计数为零；必须包含 `tests/contracts/test_controlled_real_model_config_contracts.py::test_endpoint_policy_catalog_rejects_unknown_or_mismatched_identity_before_runtime_side_effects` 与 `tests/contracts/test_controlled_real_model_config_contracts.py::test_model_catalog_rejects_unknown_mismatched_or_self_reported_bounds_before_reservation`。
- [ ] 1.4 实现 deployment-aware `ModelSettings`、credential typed secret、typed endpoint/model catalogs、`config/model_endpoints.py` 的 URL normalizer/版本化只读 provider-default endpoint catalog，以及 `config/model_catalog.py` 的模型目录解析、规范 Decimal 与 canonical digest；更新公共 config exports，让 1.2/1.3 红灯转绿且不削弱既有 CFG-001 direct/`_FILE` 冲突、secret-root 与 fake 离线规则。
- [ ] 1.5 把 local/service profiles、`.env.example` 和配置 fixtures 迁移到共享 canonical `fake_default` deployment；其 allowed models 覆盖所有已提交模板 Agent 的 default/fallback models 与 `fake-scaffold`。真实示例只保留非敏感、默认不启用的品牌字段，并验证默认配置不需要真实 credential。

## 2. Registry 与只缩权 Route TDD

- [ ] 2.1 新增 Agent model policy 红灯合同，覆盖 `deployment_id`、`allowed_models`、default/fallback 子集、public descriptor 脱敏和所有模板 Agent config 迁移；在 `tests/contracts/test_agent_scaffold_validation_atomicity_contracts.py` 冻结 `test_scaffold_generated_fake_policy_satisfies_strict_registry_and_runs_offline`，证明 scaffold 输出 `fake_default`/`fake-scaffold` 策略后通过正式 Registry、local/service profile 路由与离线 runtime，且零真实 credential/网络。
- [ ] 2.2 新增 route 红灯合同，覆盖 deployment ∩ Agent ∩ request、`provider_kind=Agent/provider assertion=route=adapter provider_id` 唯一身份、`pydantic-ai` 私有实现名不外泄、空交集/未知 deployment/model/capability、DTO 对 endpoint/credential extra 返回 `model.request_invalid`、plan 后 mutation 不影响结果，以及拒绝早于预算/授权成功 evidence/provider call；必须包含冻结节点 `test_provider_identity_assertion_matches_deployment_kind_and_bound_adapter`。
- [ ] 2.3 新增 price/fallback 红灯合同，覆盖 cost hard limit 缺少完整价格 identity 时 `budget.reservation_rejected`、fallback 只在冻结交集内并重新经过 hard eligibility/budget，以及真实失败不自动切 fake。
- [ ] 2.4 新增 shared-budget snapshot 红灯合同，在 SQLite 与 PostgreSQL 覆盖 exact `budget-tree-v2`、完整 v1 fake-only projection、id/schema mismatch、v1 real provider、partial/corrupt payload 与禁止读取 current settings 补齐；必须包含 `tests/contracts/test_controlled_real_model_budget_snapshot_contracts.py::test_budget_tree_v2_repository_validator_freezes_deployment_and_allowed_models`，直接驱动共用 storage validator。
- [ ] 2.5 实现扩展后的 `AgentModelPolicy`、registry loader、`runtime/shared_budget.py` 投影与 `storage/_shared_budget_repository_records.py` 共用 validator 的 v1/v2 fail-closed 规则；同步更新 `scaffold_templates.py` 生成必填 deployment/allowed-model 字段，并在 `_scaffold_support.py` 校验 provider、`fake_default`、`fake-scaffold` 与 default/fallback 子集。更新所有 Agent fixtures，让 2.1/2.4 转绿且旧 snapshot 不从当前 real config 猜测路由。
- [ ] 2.6 实现 provider-neutral `ModelRouteIntent`/frozen `ModelRoutePlan` 和新 router 规划顺序，让 2.2/2.3 转绿；route plan 必须冻结 deployment/provider/model、endpoint 与 model catalog ref/version/digest、request-shape、origin/credential identity/capability/受信 envelope 与 price/deadline/retry/completion-classifier/bulkhead、每 attempt bounds、`max_attempts`、checked reservation bounds 与 snapshot version。Unknown/mismatch 或 deployment 自报目录值必须在 reservation/client/network 前拒绝。

## 3. Async Provider 与执行策略 TDD

- [ ] 3.1 把 provider/router 调用合同先改为 async 红灯，参数化验证 fake provider 仍确定性离线、现有 invocation/usage settlement 仍只产生一次 started/final evidence。
- [ ] 3.2 新增 Pydantic AI adapter 成功路径红灯合同，使用 async Agent/client/transport doubles 验证 lazy factory 在合法 route 的 reservation/permit 后才构造、显式 `openai-compatible`、单 user prompt且无 instructions/history/tools、`ModelSettings(max_tokens=plan.output_token_cap)` 逐字传入、纯文本 output、usage/latency/route/attempt 归一化、无 vendor 对象越界和零 hidden SDK retry；先在 `tests/contracts/test_vendor_boundary_doctor_contracts.py` 建立 `openai` 只能从仓库相对前缀 `packages/agent-harness/src/agent_harness/adapters/` 导入的红灯，并锁定 `templates/service-app/agents/**/adapters/**` 与 `templates/service-app/app/integrations/**` 等误导性同名目录不得绕过，再更新 `contracts/boundaries.py` 与 `scripts/import_boundary_check.py` 的精确前缀判定。依赖红灯同时要求核心包直接声明 `openai>=2.44.0,<3`、lock 仍解析 `2.44.0` 且 207 项 `(name,version,source)` identity 不变、Apache-2.0 policy basis 为 `https://pypi.org/project/openai/2.44.0/`。必须包含 `test_openai_sdk_ambient_env_cannot_change_controlled_client_or_outbound_request`，同时注入全部已知 `OPENAI_*` identity/header env，断言出站 endpoint/header/client identity 只来自 typed plan且不修改进程环境。
- [ ] 3.3 新增 retry 红灯合同，使用可编程 transport/clock 覆盖默认/未绑定 classifier 的 429/5xx 不重试、受信 endpoint policy/version 下 exact 单值 `X-Agent-Harness-Completion-State: not-started` 才产出 false 并重试、缺失/重复/逗号合并/多值/畸形/body/exception 伪造不重试、response id/usage/partial result 覆盖 false、connect-before-send、`Retry-After` 上限、backoff、attempt 上限、剩余 total deadline、含糊错误与准确 call count；必须包含冻结节点 `test_retry_requires_trusted_versioned_completion_signal`。
- [ ] 3.4 新增 `tests/contracts/test_controlled_real_model_policy_approval_contracts.py` 与 provider-neutral permit/deadline/cancel/Bulkhead 红灯合同，逐边界锁定 hard eligibility → exact `PolicyCheck(action=model.invoke, resource=agent:<agent_id>:model)`/fallback/approval 协调与既有 `policy.decision` audit → reservation → permit → client lease → durable mark → send。覆盖 allow/deny/require-approval audit 不冒充 provider started；require-approval 复用既有 `AgentApprovalRequest`、`policy_approval` checkpoint、`ApprovalRecord`、resolution lease、`ApprovalGrant`、`ApprovalService` 与 continuation，等待时 reservation/permit/client/mark/network 全为零；全绑定 grant 经 `BoundModelInvocationService.complete_approved()` 单次消费、重新检查 route/catalog/current hard balance并最多调用 provider 一次；mismatch/stale/duplicate/replay 在副作用前 fail closed。另覆盖 dynamic reject 三类计数为零、queue timeout 在 client/mark 前零网络、client 构造失败回滚、permit 后 client 前崩溃可重放、client 后 mark 前失败/崩溃不伪造 started、mark 前取消回滚并返回 `model.invocation_cancelled`、mark 后 send 前/attempts 间崩溃 unknown、connect/read/total timeout、retry wait deadline 和外部 `CancelledError`。必须包含 `test_model_policy_coordinates_and_audit_precede_reservation`、`test_require_approval_creates_durable_checkpoint_with_zero_model_side_effects`、`test_bound_approval_grant_rechecks_hard_budget_and_invokes_provider_once`、`test_mismatched_stale_or_replayed_grant_fails_closed`、`tests/contracts/test_controlled_real_model_retry_budget_contracts.py::test_execution_order_and_each_pre_send_failure_boundary_are_fenced` 与 `tests/contracts/test_controlled_real_model_retry_budget_contracts.py::test_pre_mark_cancel_rolls_back_reservation_permit_and_client_without_network`。
- [ ] 3.5 将 `ModelProvider.complete()`、`ModelRouter.execute()`、兼容入口 `ModelRouter.route()` 与 `packages/agent-harness/src/agent_harness/adapters/models/fake.py` 的 fake provider 演进为 async seam，迁移调用方并删除线程池 timeout 旁路，让 3.1 转绿。`route()` 只能 plan 后 `await execute()`，不保留同步桥接。精确迁移 `design.md` / change matrix 冻结的 18 个既有同步 provider double、同步 Pydantic AI adapter seam、直接 adapter caller 或 route caller 文件：先保留因 coroutine/await、`Agent.run()` 或 deadline 合同不匹配产生的可解释红灯，再将 doubles 改为 `async def`、直接 adapter/route 调用改为 await，并迁移 `test_model_usage_provider_adapter_contracts.py` 的 `run_sync()` / `_run_sync_with_timeout` 替身，同时保留 timeout secret 脱敏、成功 usage 持久化和 missing-usage 断言；如发现第 19 个必须修改的既有测试，先回到契约补所有权并重新 review。
- [ ] 3.6 先把当前 lock 已有的 `openai==2.44.0` 以 `openai>=2.44.0,<3` 写入核心 `pyproject.toml`，更新 `uv.lock` 直接依赖关系而不改变任何 package identity，并补 `compliance/third-party.toml` Apache-2.0 条目；随后在唯一 vendor adapter 内实现锁定 `pydantic-ai==2.5.0` / `openai==2.44.0` 的私有 `ControlledOpenAIClientFactory`、client lease registry 与出站 header/origin allowlist transport。显式构造 `AsyncOpenAI`/`OpenAIProvider`/`OpenAIChatModel`/`Agent.run()`，传 typed key、空 admin/org/project/webhook、冻结 base URL/空 default headers，设置 `max_retries=0`、`retries=0`、`trust_env=False`、`follow_redirects=False`，确保 SDK ambient env 不能改变出站 identity，让 3.2 转绿。
- [ ] 3.7 实现 `PreparedModelCall`/`ModelExecutionPermit`、版本化 completion classifier、有界 retry、total deadline、process-local Bulkhead、`ModelAttemptEvidence` 与安全 `ModelProviderError`；把 model policy coordination 接入既有 `policy/engine.py`、`audit/service.py`、`runtime/executor.py`、`approvals/{service,_continuation}.py`、`runtime/{continuation,_run_continuation}.py` 和 `models/invocation.py`，不得新增第二套审批状态机或 public approval bool。让 3.3/3.4 转绿；所有 attempts 复用同一 permit/reservation/client lease/mark，transport 只向核心暴露 safe status/Retry-After/completion enum，raw body/header/exception/secret 不得进入核心 DTO。

## 4. Durable Usage、预算与恢复 TDD

- [ ] 4.1 新增 invocation/settlement 红灯合同，覆盖 API Contract 5.29 的 route/attempts/budget_charge、顶层 DTO 不漂移、route/evidence/`budget-tree-v2`/operation identity 冻结同一 model catalog ref/version/digest 与解析值、prompt UTF-8 bytes+catalog envelope 输入上界、request/deployment output cap、input/output price 精确公式、unknown/mismatch/自报低报或高报与超界 prompt 在 reservation/client/network 前拒绝且三类计数均为零、adapter double 收到冻结 cap、一次调用只按 checked `per_attempt_bound * max_attempts` 建 reservation，以及逐维度 `not_started=0`、started-with-actual、started completed/failed/retryable-without-usage 保留原 reservation并 needs-review、unknown/invalid/actual-over、cost-disabled null、只有全量可信 actual 才聚合退款；必须包含冻结节点 `test_invalid_dynamic_route_rejects_before_reservation_client_and_network`、`test_route_reservation_bound_and_adapter_output_cap_are_enforced_before_send`、`test_retry_attempts_reserve_and_settle_only_trusted_actual_usage` 与 `test_started_completed_or_failed_without_usage_keeps_reservation_and_fences_terminal`。
- [ ] 4.2 新增 cancel/unknown/replay 红灯合同，覆盖 durable mark 前取消按 `not_started` 回滚 reservation/permit/client 并返回 `model.invocation_cancelled`、mark 后 timeout/cancel/crash 按 unknown 保留 reservation、非法/矛盾 attempt 组合 fail-closed、顶层 token 聚合或 null、不把最终一次冒充总 usage、保守 estimated cost、terminal fencing、sink crash recovery 只补投同一 event id 且 provider 不重放。
- [ ] 4.3 更新 `ModelInvocationService` 与 settlement error mapping，持久化安全 route/attempt/outcome/charge，区分 `model.bulkhead_saturated`、`model.invocation_cancelled`、`model.provider_failed`、`model.provider_retry_exhausted` 与 `model.provider_side_effect_unknown`，让 4.1/4.2 在 SQLite 路径转绿。
- [ ] 4.4 在真实 PostgreSQL repository/worker contract 中验证相同 route/attempt/unknown/recovery 语义，不新增 migration；若测试证明必须增加持久化列，停止实现并回到 proposal/design 拆分 migration。

## 5. Composition、生命周期与离线门禁 TDD

- [ ] 5.1 新增 runtime composition 红灯合同，验证 fake/真实 deployment 按 id 全有或全无注册、startup 只注册 lazy factory且 client count 为零、invalid 静态 endpoint/credential/capability/model catalog/price 在接受 run 前失败、invalid 动态 route 的 reservation/client/network count 均为零，真实 factory 只能取得已解析的 endpoint/model catalog blueprint，业务 Agent/template/vendor import boundary 不泄漏 SDK。
- [ ] 5.2 新增资源生命周期红灯合同，验证 API lifespan、worker、CLI、factory 构造中途失败和 startup 中途失败都幂等关闭已构造 client leases，未构造 factory 可无副作用关闭，且关闭发生在 storage dispose 前。
- [ ] 5.3 在 production composition 前新增默认网络哨兵红灯合同，运行 unit/contract/eval/smoke-local 时即使存在 provider 原生 key/admin/org/project/webhook/custom-header/base-url/proxy env 也保持 typed config 不受影响、零真实 credential 使用和零外部 DNS/HTTP；合法 adapter transport 合同另证明这些 ambient 值不能改变出站 identity/header/origin。冻结节点必须先于 5.4 失败并保存证据。
- [ ] 5.4 实现 `runtime/services.py` 的 deployment provider factory、router/shared-budget/policy/approval continuation 装配和 `ModelInvocationService`/router/provider `aclose()` 链路，更新 template `RuntimeComponents.close()`，让 5.1/5.2/5.3 转绿并修复所有 fake/local 回归。
- [ ] 5.5 先为 `model-live-smoke/v1` 输出、授权/opt-in/credential/endpoint 四条件、状态/退出码映射和零 ambient env 建立红灯合同，再实现固定入口 `scripts/smoke_live_model.py`、`make smoke-live-model`/`ci-smoke-live-model`、`ci_evidence.GATE_TARGETS`、acceptance known/required producer 与 evidence mapping；同时在 GitHub/GitLab 各增加实际 producer job、上传 `.artifacts/ci/smoke-live-model/result.json`，并让两套 required `acceptance-validate` 显式 needs/download/消费该 artifact。当前未授权时必须输出 `hosted-unverified`、`provider_called=false` 并映射 `ci-result/v1 status=skipped`，不得伪造 PASS。

## 6. 文档、验证与 ready-to-archive 证据

- [ ] 6.1 更新模板中英文 README、核心维护文档、Product Spec/DEV-PLAN/living plan/change matrix，说明 deployment/credential/origin/route/retry/Bulkhead/unknown、process-local 并发作用域、默认离线和 live opt-in 边界；只在证据已完成后勾选 AC-077 至 AC-084。
- [ ] 6.2 运行聚焦 config/registry/router/adapter/invocation/composition/SQLite/PostgreSQL 合同，记录命令、退出码、测试数、跳过项和当前 Git tree；任何失败必须修复并重验。
- [ ] 6.3 运行 `uv lock --check`、`make quality`、全量测试、独立 `make eval`、`make smoke-local`、因 composition 变更必需的 `make smoke-service`、`make build` 与 `make license-check`，分别记录命令、退出码与冻结 evidence；`make eval` 还必须记录 eval artifact 和网络哨兵结果。证明 `openai` 仍为 `2.44.0`/registry source、207 项 package identity 摘要不变且 wheel/sdist 直接 metadata 含有界依赖。默认流程必须零真实 provider 请求。
- [ ] 6.4 对 change 执行 strict validation、`git diff --check`、语言/secret/import/streaming 非目标检查；当前没有单独授权，真实 live smoke 唯一记录为 hosted-unverified/CI skipped，只有未来四项前置齐全并授权后发生外部阻断才记录 external-blocked。
- [ ] 6.5 在 fresh 实现审查 PASS 后同步任务状态与唯一下一动作，收口为 `ready-to-archive`；除实现前已获授权、只包含全票审查通过契约的 scoped 本地提交外，不执行其他 commit、push、release、deploy、OpenSpec sync/archive 或真实 provider 调用。实现提交仍需用户另行授权。
