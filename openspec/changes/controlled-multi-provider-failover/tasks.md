## 1. 契约冻结与红灯

- [x] 1.1 在 config/registry/router 公共 seam 建立 AC-090 红灯：两个不同 deployment/provider doubles、显式 `fallback_routes` 顺序冻结、chain legacy字段严格等于请求缩权前原始 Agent首-route兼容投影且不能授权后继；覆盖 Agent `[A,B]`、request `[B]`时 Agent投影/完整授权仍为 A与 `[A,B]`、candidates仅为 B。Request有序子序列缩权为单候选仍写 v1 state，legacy descriptor不隐式启用 chain，非法投影改写/新增/重排/endpoint/credential在 client/network前失败。
- [x] 1.2 在 bound invocation 公共 seam 建立 AC-091 红灯：`client_not_started` 与端点绑定 classifier+显式状态白名单+零生成计量事实证明的 `trusted_business_not_started` 各只推进下一候选一次，并覆盖三候选 `trusted_business_not_started → client_not_started → completed` 与反向顺序；另覆盖同候选两次受信 response retry 的逐 attempt proof 先追加、后 transfer、commit-ack/recovery不重发。403 默认不启用、同 route retry 先于跨 provider，含糊 timeout/response、unknown、response identity、usage、text 后后续 provider 调用为零，PolicyEngine deny 不可绕过。
  - 同一红灯必须要求每次首次调用/retry在client/send/provider边界前先耐久创建不可覆盖的global attempt started identity，并覆盖attempt 1 proof已提交后attempt 2 started mark提交、send前崩溃，以及send后proof/settlement前崩溃；两个窗口与commit-ack未知均保留reservation/needs-review，不重发attempt 2、不创建attempt 3、不调用后继。
  - 同一红灯还必须区分两条封闭顺序：legacy单route逐字保持`reservation → permit → client → durable side_effect_started → send`；显式chain固定`candidate reservation → durable attempt started identity → Bulkhead permit → candidate-isolated client/prepare → send/iterate`。Chain的permit/client/prepare失败必须以proof原子关闭已存在的started lifecycle，崩溃或commit-ack未知不得删除identity或恢复重发。
- [x] 1.3 在 shared-budget repository/UoW 公共 seam 建立 AC-092 红灯：调用级 claim单调高水位与逐候选 side-effect state分层、逐候选有序且不可覆盖的 attempt proof records、不同候选价格/上界、direct/allocation并发、两种 proof的同候选 retry混合顺序与三候选双顺序、原子 transfer、逐候选 Policy/HITL；另证明 `static_ineligible`与 `budget_ineligible/soft_budget|balance`均为零 attempt/proof/reservation，普通中间skip不单独追加transition，A当前 reservation在一个 UoW跨过预算不可用 B直达 eligible C，或在无后继时 actual-zero释放并 terminal，首次 durable决定不随后来余额漂移。Route chain在 approval前以原始可信 operation key冻结 usage call id/operation identity，首候选 approval零 impact coordination row和后继 approval暂停均保存同一身份，request/grant两阶段 binding、approval lease独立提交与 shared-budget activation durable handoff复用原 claim；成功activation只允许同ordinal`approved/approval_granted`、released=`0/null`、reserved=目标冻结bound且不得追加`activated`，commit-ack逐值重放同一sequence/数组。获批目标 balance不足保留双binding/既有waiting coordination但零reservation和零approved/activated transition，该ordinal作为跨普通skip不变的零impact source anchor；后继重新授权的allow/require-approval/deny/全耗尽分别锁定balance transfer、零bound waiting、零bound policy-denied terminal与route-exhausted terminal。初始前导skip不成为source，后续allow/waiting从null开始、deny零transition。覆盖四分支正反例、非法from/to/reason/bound、伪造 ID、`approved:<approval_id>` rekey、policy deny零 charge结算、幂等重放、unknown不退款不重放及 SQLite/PostgreSQL。
- [x] 1.4 在 bound streaming 与 committed reader 公共 seam 建立 AC-093 红灯：首 delta 前两类可信 not-started 可切换，公开 delta/completed 使用实际产出候选的全局 provider attempt；其他 started/response、含糊 timeout、delta 观察/提交后取消、deadline、断线、恢复与重连永久围栏后续 provider。
- [x] 1.5 在 composition/lifecycle/offline 公共 seam 建立 AC-094 红灯：逐 deployment endpoint、credential、client lease、catalog、Bulkhead 隔离，close/reload 恢复旧 chain，真实链失败不隐式 fake，默认 fake/local 网络调用为零。
- [x] 1.6 在 live smoke/CI artifact 公共 validator seam 建立 AC-095 红灯：双受信`deployment_id`、双隔离`credential_ref`、双受信endpoint（`provider_kind`可相同）、授权/opt-in/受信 not-started fixture 的完整前置；加入同kind双deployment可达PASS正例与任一deployment/credential/endpoint复用拒绝负例；验收route各`max_attempts=1`；两类 request/response/reason/status exact组合；`[1,2]`连续唯一ordinal、`selected_ordinal=2`→唯一completed、candidate/top-level attempt count与proof count绑定；usage非bool token、封闭cost status与null组合；artifact逐值绑定durable chain/attempt/proof/usage evidence。覆盖unknown/重复/零ordinal、`selected_ordinal`缺失或不存在、completed重复、fractional/bool/负token、任意cost status、cost null冲突、attempt/proof count不一致、缺失原因固定优先级、零调用hosted-unverified、chain冻结前failed空identity、冻结后failed非空identity、两类failed混搭拒绝、external-blocked与本地contract failure分层。
- [x] 1.7 运行上述精确 pytest nodes，保存实现前可解释 RED；若现有代码意外通过，修正 public seam 前提而不是写私有 helper/空断言。

## 2. Route refs、冻结链与快照恢复

- [x] 2.1 在 `config/schemas.py`、`registry/descriptor.py` 与 `registry/_loader.py` 实现最多 8项的显式 `ModelRouteRef`/`fallback_routes`；旧同 deployment policy只保留 legacy planning。Chain mode继续接收旧必填字段但严格校验为首候选确定性投影（首 provider/deployment/default、首 deployment有序 allowed、空 fallback），public summary保留完整 refs，并拒绝任何投影扩权或跨候选字段借用。
- [x] 2.2 在 `models/providers.py` 与 `_router_contracts.py` 实现 request `route_refs`、frozen `ModelRouteCandidate`/`ModelRouteChainPlan`、去敏 public identity，并在 `models/__init__.py` 维护公共 route-chain DTO 导出；所有 route-chain digest共用`model-route-canonical-json-v1`唯一 serializer。实现`model-route-chain-id-v1` exact preimage，并让 current-config/snapshot、direct/allocation、SQLite/PostgreSQL、approval handoff和公开evidence validator共用active delta列明的attempt、两类proof、ASCII及非ASCII approval request、approval grant六组逐字节golden vectors；非ASCII向量必须证明`参数`保持原文UTF-8且`ensure_ascii=true`的错误hash关闭失败。覆盖 Agent `[A,B]`/request `[B]`仍使用 A投影与完整 `[A,B]`授权的正例、错误 B投影、键序、unknown/缺失/null省略、Unicode转义、bool数字、float/NaN/Inf、canonical Decimal、request/grant交叉绑定、候选删除/插入/重排、权威字段变化和 current-config补值拒绝，保持旧单 route DTO兼容。
- [x] 2.3 在 `_router_current.py`、`_router_current_chain.py` 与 `router.py` 实现 `plan_chain()`：逐候选解析 deployment、catalog、endpoint、credential、capability、deadline/retry、Bulkhead、公式和 Agent/request 有序交集，冻结静态 skip 与 `model.route_chain_exhausted`。
- [x] 2.4 在 `_shared_budget_snapshot.py`、`_shared_budget_recovery.py`、`_router_snapshot.py` 与 `_router_snapshot_chain.py` 冻结/恢复完整 route refs 与 candidates；证明 reload 后旧 run 不读取当前 deployment/credential/price 补齐或重排。
- [x] 2.5 运行 AC-090/config/routing/snapshot 聚焦测试并转绿，同时回归 Phase 18/18.1 的单 deployment route 与 streaming routing contracts。

## 3. 持久化 chain state 与预算原子转移

实现 owner 补充：`models/_route_chain_state.py` 为兼容入口，`models/_route_chain_state_initial.py`、`models/_route_chain_state_approval.py`、`models/_route_chain_state_attempts.py`、`models/_route_chain_state_completion.py` 分责承载状态变换；`storage/model_route_chain_state.py`保留DTO和恢复公共入口，`storage/_model_route_candidate_validation.py`承载候选 exact-shape 与交叉不变量校验，`storage/_model_route_chain_recovery.py`承载恢复授权的完整历史语义重放，`storage/_shared_budget_route_chain_repository.py`、`storage/_shared_budget_route_chain_validation.py` 与 `storage/_shared_budget_replay_repository.py` 分别承载原子仓储协调、封闭单调性校验和durable replay seed。仓储合同按职责拆为 `test_shared_parent_budget_route_chain_repository_contracts.py`、`test_shared_parent_budget_route_chain_repository_guardrail_contracts.py`、`test_shared_parent_budget_route_chain_allocation_contracts.py`、`test_shared_parent_budget_route_chain_allocation_cleanup_contracts.py`、`test_shared_parent_budget_route_chain_transition_contracts.py` 与 `test_shared_parent_budget_route_chain_candidate_state_contracts.py`；`test_controlled_multi_provider_failover_postgresql_candidate_state_contracts.py`锁定真实PostgreSQL对称边界，`test_controlled_multi_provider_failover_recovery_contracts.py`锁定proof-transfer与后继恢复，`test_controlled_multi_provider_failover_stream_cancellation_contracts.py`锁定stream取消结算；`controlled_multi_provider_failover_settings_test_support.py`与`controlled_multi_provider_failover_test_support.py`分别持有typed配置和provider/bound装配。该拆分与恢复修正不改变公共DTO、transition table、测试节点逻辑或既有验收断言。

- [x] 3.1 新增不可变 `ModelRouteChainPlan(model-route-chain-v1)` 与可变 `ModelRouteChainState(model-route-chain-state-v1)` exact-shape DTO和 migration `0017_model_route_chain_state`，state固定包含 64位 usage call id与 operation identity digest；candidate state精确加入 `static_ineligible`与 `budget_ineligible`，后者 reason只允许 `soft_budget|balance`，两者均以零 attempt/proof/reservation/观察事实锁定跳过，普通skip不单独追加transition。实现封闭transition table与source-anchor规则：尚未激活为null、active reservation为active ordinal、获批balance不足为该零impact ordinal，前导/中间普通skip不改anchor；state/reason/from/to/released/reserved组合逐值唯一。`approved`只作transition标签，成功activation固定同ordinal、`reason=approval_granted`、released=`0/null`、reserved=目标bound并直接waiting→active，不得追加第二条activated。获批balance skip保留双binding与waiting coordination但禁止本ordinal reservation和approved/activated transition，后继allow/waiting/deny/exhausted四分支均有唯一tuple；初始前导skip后的allow/waiting从null开始、deny零transition。禁止将skip编码为 pending/denied/not_started，并覆盖初始全不可用空 transition、运行后 current→null耗尽、waiting→approved→active、waiting→balance→四分支及commit-ack exact replay。为 direct/allocation row增加 nullable state并同步 `storage/_shared_budget_repository_records.py` 的 ORM→领域记录映射，实现 SQLite/PostgreSQL upgrade、null-only downgrade、non-null completed/cancelled/active/needs-review降级关闭失败和旧 row零改写合同。
  - State exact shape同时固定全局连续不可覆盖的`attempt_lifecycle[]`：每项含candidate/global attempt、按统一canonical serializer和`d5591241…`golden vector锁定的`model-route-attempt-identity-v1` digest、`started|not_started_proven|unknown|settled`、单调观察位与nullable proof digest；identity字段不可变，只允许started原子关闭到三个终态且终态不可降级。
  - Candidate enum另加入`cancelled/reason=invocation_cancelled`，candidate/lifecycle side-effect允许可信terminal `result_committed`。该终态只映射完整stopped actual取消：selected/active/waiting为空、current reservation为canonical空、evidence ordinal命中当前candidate、既有transitions不增加；migration downgrade把non-null cancelled与completed/active/needs-review同样视为受保护数据。
- [x] 3.2 在 `runtime/_shared_budget_identity.py` 将`OperationIdentity`/shared-budget identity实现为封闭v1/v2联合：legacy单route/embedding逐字保持`budget-operation-v1`与旧canonical hash；显式chain强制`budget-operation-v2`、参与hash的digest/count及ordinal 1 provider/model/price/bound兼容投影。覆盖v1夹带v2字段、v2缺字段、跨版本同key、digest/count/ordinal1字段漂移、余额变化后重选以及旧null state exact replay正反合同。
- [x] 3.3 在 direct/allocation repository 实现对称 proof append 与 `transfer_model_route_reservation`：owner lock/CAS 下分离调用级 claim、candidate 聚合高水位与逐 attempt 事实；下一次同 route retry 前先以全局 attempt 不可覆盖地追加 proof，transfer 要求完整 proof list覆盖当前候选全部实际 attempts且无悬空 attempt。按冻结 ordinal消费绑定的 policy结果并原子扫描 current balance，将中间 soft/balance失败者标记 budget-ineligible，直接替换为首个 eligible reservation；无后继时同一 UoW actual-zero释放并 terminal。两类proof分别强制`9acc29f4…`与`fe2a4837…`golden vector；覆盖exact bytes/hash、字段篡改、同候选两次 retry混合顺序、三候选双顺序、A→C预算 skip、初始/运行后全耗尽、单调 request/response事实、同参 replay、commit-ack、CAS冲突、恢复时余额变化不重选，以及其他 started/response/unknown/delta fence fail closed。
  - Repository还必须在每次client/send/provider边界前先耐久append started identity；proof append与同一lifecycle的`started→not_started_proven`在一个UoW提交。Transfer只接受当前candidate全部lifecycle均已证明not-started并与proof一一匹配；started/unknown/settled冲突项一律拒绝。
- [x] 3.4 扩展 replay/lifecycle/recovery validator：未发送的 active candidate只恢复同 ordinal；claim高水位 started+当前 candidate not_started只在所有前序 started candidate的全部实际 attempts都有完整 actual-zero proof records/transition时合法；已提交 proof append/transfer/budget-ineligible/terminal可按同参重放，恢复先读取 durable candidate决定，只进入下一尚未开始的全局 attempt，不按 current balance重新选择。缺失/覆盖/重排 proof、悬空 started/HTTP response、含糊 timeout、unknown/delta-fenced保留 reservation并 needs-review或按可信 actual结算，result committed只补投且不重放 provider。
  - “下一尚未开始”唯一指对应attempt identity尚不存在；恢复必须先创建started record再进入边界。Started mark提交后send前、send后proof/settlement前或commit-ack未知都恢复同一identity并needs-review，不重发、不创建下一attempt、不推进provider；identity冲突、lifecycle缺口/重复/降级均`budget.operation_conflict`。
- [x] 3.5 实现逐候选 Policy/HITL：在 bound invocation、`runtime/continuation.py`、`runtime/_run_continuation.py`、`approvals/service.py`、`approvals/_continuation.py`、`storage/access_repositories.py`、`storage/approval_records.py`、`storage/approval_recovery_repositories.py` 与 `storage/service_approval_repositories.py` 复用既有 approval UoW/lease fencing。Chain首次入口在 policy前生成 usage call id与 operation digest并写私有 checkpoint；首候选 require approval按ASCII `802a004b…`及非ASCII `20dfca2b…`判别vectors写同 ID的零 impact durable carrier和 request digest，approval record/lease独立提交后，shared-budget只接受request bytes hash逐值匹配且按`d743ba66…`vector生成的grant digest，再以同一 ID/request/grant digest CAS重检 current balance；足够则按canonical approved tuple激活原 claim，不足则以 budget-ineligible零 impact且零approved/activated transition收敛、保持该source anchor并让后继重新授权，均不声称消费 lease。覆盖exact bytes/hash、UTF-8原文与错误Unicode转义、nullable字段不省略、request/grant交叉绑定篡改、allow→require approval→approve、approved transition精确from/to/reason/released/reserved、重复activated拒绝、approved→balance skip→后继allow/require-approval/deny/exhausted四分支、初始前导skip→非首ordinal allow/approval/deny、allow→deny原子释放/零 charge settlement、waiting零 reservation、stale/mismatch/duplicate/takeover grant、伪造 call ID、禁止 legacy approval rekey、waiting→record、lease→activation、activation commit-ack逐值重放、provider started与 resolution publication crash windows、冻结 snapshot恢复 exactly once；运行 AC-092 direct/allocation SQLite/PostgreSQL聚焦测试并回归既有 legacy approval/shared-parent-budget合同。

## 4. 普通 completion 候选执行与证据

实现 owner 补充：`models/_invocation_chain.py` 聚合 `_invocation_chain_base.py`、`_invocation_chain_routing.py`、`_invocation_chain_approval.py`、`_invocation_chain_completion.py`、`_invocation_chain_stream.py`、`_invocation_chain_stream_support.py`、`_invocation_chain_stream_terminal.py` 与 `_invocation_chain_evidence.py`；`_invocation_execution.py` 保留公共执行入口，`_invocation_planning.py` 与 `_invocation_chain_settlement.py` 分别承载冻结规划和 chain 初始结算。Completion合同按职责拆为 `test_controlled_multi_provider_failover_invocation_contracts.py` 与 `test_controlled_multi_provider_failover_policy_budget_contracts.py`，既有验收节点名称保持不变。该职责拆分不改变 bound façade、错误码、事务边界和冻结 ordinal 顺序。

- [x] 4.1 在 `_invocation_execution.py` 引入 provider-neutral candidate controller，把每个 candidate 的 policy、budget claim/transfer、started、permit、client/prepare、send 与 stop/advance 条件按冻结 ordinal 串行闭合。显式chain严格执行`reservation → attempt-start → permit → client/prepare → send`；legacy单route保持原顺序，不复用chain controller改写其Phase 18行为。
- [x] 4.2 在 `adapters/models/pydantic_ai.py`、`_pydantic_ai_client.py`、`_pydantic_ai_streaming.py` 与 fake adapter 中传播精确 client/send 边界、claim/candidate 高水位和逐 attempt side-effect/request/HTTP-response-observed 事实；实现 endpoint-bound `trusted_response_header_not_started/v1`、`cross_provider_failover_http_statuses`、逐 attempt proof digest和同 route retry 优先级，禁止状态码、异常文本或 body 单独授权跨 provider切换。
  - Adapter/controller必须在任何client构造、send或stream iterate前消费已提交的attempt started identity；不能以candidate/claim聚合started替代本次identity，也不能在started record存在时恢复重发。
- [x] 4.3 扩展 `_invocation_evidence.py`、usage DTO、`_settlement_contracts.py`、`_settlement_validation.py`、`_settlement_evidence_validation.py`、`_settlement_evidence_models.py` 与 `_settlement_chain_evidence_validation.py`，字段级冻结 `decision.route_chain={schema_version,identity,state}`、保留既有 route/attempts/budget_charge，并明确不新增顶层 `ModelUsageEvidence.usage_call_id`、只接受与 CanonicalEvent correlation逐值相同的嵌套 `decision.route_chain.state.usage_call_id`；以始终非空的 `evidence_route_ordinal` 唯一映射 active/waiting/completed/cancelled/denied/exhausted/unknown。为 chain attempts增加 request/response/status/观察位、endpoint/classifier identity与 nullable not-started reason/proof digest，并与 candidate有序 proof records逐值交叉验证。覆盖 static/budget-ineligible与not-started互斥、两类 skip的 reason/零 attempt形状、全局连续 attempt、proof缺失/覆盖/重排、unknown fields、旧新混搭、非法组合、负数、bool数字、NaN/Inf与 transition篡改。
  - 扩展映射还须覆盖cancelled：final attempt固定`outcome=cancelled/side_effect_state=started/completion_observed=false`、candidate/lifecycle/claim为result_committed、actual charge和`model.invocation_cancelled`逐值一致，selected为空且无completed；`provider_called`只由明确request/response/result/usage/text/delta事实推导，prepared或cleanup失败不得改写。
  - 同时逐值校验`attempt_lifecycle[]`与公开attempts/candidate proofs：started identity缺失/重复/重排/跨candidate、identity digest漂移、非法终态降级、proof只写一侧或unknown/settled携带proof都在任何publication前关闭失败。
  - 以active完整`MODIFIED`的`Provider-neutral permit 固定 Bulkhead 与耐久副作用顺序`覆盖主规格：legacy保留permit/client→mark及复用mark的既有语义；chain每次retry独立执行reservation→attempt-start→permit→isolated client/prepare→send，禁止把两种模式合并成一条无条件顺序。
  - 以active完整`MODIFIED`的`重试只基于显式完成状态并受同一 deadline 约束`逐项保留主规格Scenario并分流terminal：legacy retry耗尽继续`model.provider_retry_exhausted`且不得透出最后一次内部`model.provider_failed`；chain只有全部attempt proof-close且deadline未耗尽时才可在retry耗尽后transfer，无后继为route-chain exhausted，timeout/unknown绝不跨provider。
- [x] 4.4 扩展 `_invocation_settlement.py`、`_invocation_chain_settlement.py`、`_settlement_publication.py`、bound `complete_approved()`/`stream_approved()` 与 shared-budget recovery，使 not-started transfer、approval waiting/resume、成功 actual settlement、unknown needs-review、exhausted error和真实链零 fake都复用初始 stable usage operation/claim/outbox；从私有 checkpoint重算 identity，禁止 approval id rekey且可重放。
  - Chain approval续跑由`_invocation_approval_identity.py`从durable approval metadata/checkpoint恢复原usage call、operation、chain和waiting ordinal，并与durable route-chain state逐值校验；不信任调用方operation key、不读取current fallback policy。Legacy单route继续使用`approved:<approval_id>`。Direct/allocation与close/reload正反合同由`test_controlled_multi_provider_failover_approval_checkpoint_contracts.py`承载。
- [x] 4.5 运行 AC-091/AC-094 completion、composition、close/reload、usage/evidence/recovery 聚焦合同并转绿，回归 Phase 18 completion/retry/deadline/offline contracts。

## 5. Streaming 首 delta 围栏

- [x] 5.1 在 `_invocation_streaming.py` 复用 candidate controller；由 `_streaming_events.py` 在首 delta intent UoW 同时写入 durable `delta_fenced=true`，首个 provider delta 观察时立即设置内存 fence，确保除完整 `trusted_business_not_started` 外的 started/HTTP response 后都不能切换。
- [x] 5.2 扩展 streaming consumption/settlement/recovery，使首 delta前两类可信 not-started可原子 transfer，并在同一 UoW跨过零 attempt的 static/budget-ineligible中间候选直达首个 eligible后继或安全 exhausted；其他 started/HTTP response、含糊 timeout、delta观察或持久化失败统一停止或 unknown，既有 committed prefix、65槽 stream/usage capacity、budget与 lease保留。
  - 覆盖下一stream attempt started mark提交后send前、send后proof/settlement前两个SQLite/PostgreSQL crash窗口；即使尚无delta或`request_sent=false`也不得重发、创建下一attempt或切provider。
  - 实现前必须以active `MODIFIED` runtime composition与stream prepare requirements锁定公共seam：chain在permit/client/prepare前已有started identity，legacy仍在client后写既有side-effect mark；不得让两条顺序在composition或recovery中互相覆盖。
  - 同时以active完整`MODIFIED`覆盖`流式 provider 关闭结果必须可分类`、`副作用前的双容量预留`和`中断、unknown 与恢复`：legacy关闭/取消/恢复逐字保持；chain内部candidate准备失败保留调用级容量并proof-close/transfer，显式取消不授权fallback，只有durable transfer已提交且后继identity不存在时recovery才可启动冻结后继一次。
  - Route-chain显式取消/deadline只有在close result证明`stopped + complete usage`且完整durable group无delta intent/发布不确定性时，才以同一UoW按actual收敛`cancelled/invocation_cancelled`、取消未使用槽位并释放相应容量；not-started、partial/null/unknown、close失败或durable delta不确定性仍needs-review，且稳定安全错误不被cleanup异常覆盖。
  - `中断、unknown 与恢复`还必须保留Phase18.1的delta result-persisted补投、durable delta公开失败保留完整65槽和单一绝对deadline三类场景；chain对三者一律delta-fenced且零后继provider。
- [x] 5.3 修改 Phase 18.1 `稳定身份、可见性与完成校验` 和可信绑定入口 delta，并扩展 `storage/stream_evidence_repositories.py` 与 `storage/usage_attempt_review_repository.py`：legacy审批 identity及 attempt=1保持不变；chain stream复用 approval前冻结的 usage call id，durable validator、unknown/needs-review attempt、delta/completed都使用实际产出或未决候选的全局 provider attempt且 event id不变。证明 waiting/resume不新建 stream group、伪造/rekey关闭失败、attempt 2/3可持久化并恢复、非法降回 1关闭失败，SSE/CLI reader、`Last-Event-ID`与 reconnect仍只读 committed CanonicalEvent，不持有 provider cursor、不触碰 chain controller或推进 ordinal。
- [x] 5.4 运行 AC-093 streaming success/cancel/deadline/backpressure/recovery/transport/PostgreSQL 聚焦合同并转绿，回归 Phase 18.1 全部 streaming contracts。

## 6. Composition、live smoke 与交付接线

- [x] 6.1 先按 typed-config 的 `MODIFIED` delta 消除“真实 provider 只允许非流式”主规格漂移，再更新 `runtime/services.py` 与模板 composition，按多 deployment 构造无 secret provider façade、deployment-isolated lazy client/Bulkhead，并保持 startup client count=0 与默认 fake/local 零网络。
- [x] 6.2 实现 `model-failover-live-smoke/v1` 判别联合、artifact/durable-evidence交叉 validator 与 `make smoke-live-model-failover`/`make ci-smoke-live-model-failover`；前置不足必须零调用 hosted-unverified，不读取/输出 credential；共享`scripts/smoke_live_model.py`须透传稳定错误的`failure_domain`，runtime/缺失域或本地预算、恢复、发布失败固定为failed/contract_failure并保留冻结耐久事实，只有显式provider域的封闭外部错误才允许external-blocked。validator按status封闭 `reason_code`：hosted只允许`authorization_missing|failover_opt_in_missing|credential_pair_missing|deployment_pair_invalid|not_started_fixture_missing`，external只允许`network_unavailable|provider_rejected|quota_blocked|provider_timeout|provider_result_unknown`，failed只允许`contract_failure`，passed只允许null。只有恰好两个不同deployment、隔离credential与endpoint（provider kind可相同）、两次全局attempt、ordinal `[1,2]`、首项完整proof和次项唯一completed/usage逐值一致才允许 passed，完整前置后的外部故障才允许 external-blocked。
- [x] 6.3 接入 `scripts/ci_evidence.py`、Makefile、GitHub/GitLab CI 与 `compliance/ci-jobs.toml`，让 test/integration/smoke-service/live producer 生成可验证 artifact，required acceptance job 消费对应 evidence。
- [x] 6.4 更新 `docs/acceptance-matrix.md`，为 REQ-027、AC-090～AC-095 列出真实生产文件、精确 pytest nodes、全部 producer 和 artifact；运行 identity/semantic/validator 合同。

## 7. 产品、API 与维护文档同步

- [x] 7.1 将 Product-Spec.md 的 FLOW-008/REQ-027 与 AC-090～AC-094 更新为当前实现事实；AC-095 只按实际 live 前置/结果记 pass、hosted-unverified 或 external-blocked，Product-Spec-CHANGELOG.md 保持产品事实而非 review 日志。
- [x] 7.2 将 API-Contract.md `MOD-003` 从规划更新为字段级已实现契约：route/request/chain/candidate/transition/evidence/error/smoke exact shape，不新增 HTTP endpoint，不泄露 SDK/secret。
- [x] 7.3 更新 DEV-PLAN.md、README.md/README.zh-CN.md、相关双语 adapter/runtime/operations 维护文档，说明配置、执行、故障分类、恢复、回滚和默认离线边界；中英文事实逐项一致。
  - 本轮最小契约修订同步说明`cancelled/invocation_cancelled`可信actual终态、prepared非调用事实、cleanup错误优先级与partial/null/unknown needs-review；不把审查过程写成产品功能。
- [x] 7.4 持续更新 living plan 与 change matrix 的最新 Git/OpenSpec 基线、Progress、Discoveries、Decision Log、风险、验证身份、唯一下一动作和 Handoff Snapshot；旧证据只保留历史身份，不冒充当前 PASS。

## 8. 最终验证、审查与 ready-to-archive

- [x] 8.1 运行全部 Phase 18.2 聚焦 public-seam contracts、适用 migration/SQLite/PostgreSQL tests、文档/acceptance/CI contracts、`make quality` 与 import boundary，修复到 PASS。
  - D-149 cleanup unknown修复后，completion/stream × global attempt 1/2四节点、allocation对称节点、受影响回归与stream/recovery均退出0；精确验收矩阵保持`280 collected / 241 passed / 39 skipped`，39项因未注入PostgreSQL DSN跳过。随后 fresh Reviewer 1 判定Stage 1 PASS、Stage 2以5个超过500有效行且职责混杂的Python文件给出`0 HIGH / 1 MEDIUM / 0 LOW`并FAIL；D-150将stream辅助/terminal收敛、state completion、candidate validation、allocation cleanup与repository guardrail拆到6个职责文件，原测试节点逻辑和断言不变。拆后五个原文件有效行分别为498、400、476、462、483；Phase18.2聚焦超集`294 collected / 255 passed / 39 skipped`、`make quality`检查774文件且Ruff、Pyright 0/0、import boundary通过。Owner manifest校准为129路径；该证据只允许重新进入8.2，不替代fresh审查或8.3重型门禁。
  - D-151前三票中Reviewer 1/2均PASS，Reviewer 3以live producer在provider已结算但terminal编排失败时丢失durable chain并误报零调用的1项MEDIUM阻断Stage 1/2，全部旧票失效。公共`run_authorized()`节点先稳定RED为`provider_called=false`，共享executor现保留正式run identity，异常分支只用该identity重算usage call并回读同一storage的route-chain state；节点转绿，live/orchestration/offline受影响集`54 passed`，`make quality`检查775文件且Ruff、Pyright 0/0、import boundary通过。新增独立orchestration合同后owner manifest为130路径；该证据只允许重新进入8.2，不替代fresh审查或8.3重型门禁。
  - D-151冻结身份`11c1844e…`的fresh Reviewer 1判定Stage 1 PASS、Stage 2以`scripts/smoke_live_model_failover.py`达到513有效行且混合durable evidence、正式composition与CLI职责的1项MEDIUM阻断。D-152将durable chain/usage读取与artifact投影移入`scripts/live_model_failover_evidence.py`，原模块继续保留`run_authorized()`、monkeypatch seam与CLI；拆后两文件有效行分别为427和108。live/orchestration/offline受影响集`54 passed`，`make quality`检查776文件且Ruff、Pyright 0/0、import boundary通过；owner manifest为131路径。该证据只允许重新进入8.2，不替代fresh审查或8.3重型门禁。
  - D-152冻结身份`860c981c…`的fresh Reviewer 1判定Stage 1 PASS、Stage 2以`_controlled_model_route(descriptor: Any)`位于已解析Agent descriptor安全边界、绕过Pyright字段漂移检查的1项MEDIUM阻断。静态类型合同先稳定RED为`'Any' != 'AgentDescriptor'`；D-153将TYPE_CHECKING import和参数收窄为真实`AgentDescriptor`，不改变snapshot payload或运行行为。组合合同`10 passed`，当前静态节点与`make quality`均通过，quality检查776文件且Ruff、Pyright 0/0、import boundary通过；owner manifest仍为131路径。该证据只允许重新进入8.2，不替代fresh审查或8.3重型门禁。
  - D-153冻结身份`72aefdf4…`的fresh Reviewer 1判定Stage 1/2 PASS、0 findings；Reviewer 3以AC-095验收矩阵漏列`scripts/live_model_failover_evidence.py`和terminal后置编排失败恢复精确节点、DEV-PLAN关键文件漏列该helper的1项MEDIUM阻断Stage 1，Reviewer 2已中断，旧票失效。D-154仅补齐任务6.4/7.3的真实生产路径、精确测试节点与状态记录，不改变行为、契约语义或131路径owner manifest；机械校验和新冻结身份完成后从fresh Reviewer 1重启，不能复用D-153票或提前运行8.3。
  - 用户要求D-154更新Spec先完成fresh Reviewer 1→并行Reviewer 2/3的Stage 1/2 PASS、0 findings，再单独amend到原契约提交；只有完成该顺序后，才以新HEAD重冻未提交实现并进入8.2，生产实现不得随Spec提交进入历史。
  - D-154/D-155更新Spec首名fresh Reviewer 1以Handoff Snapshot预先宣称amend已完成的1项HIGH阻断Stage 1；D-156只把该行改为Git事务尚未完成、amend后才重冻实现，不改变行为、验收映射或owner范围，旧票失效并从新的Reviewer 1重启。
- [ ] 8.2 冻结最终内容身份，先由1名fresh code-reviewer完成Stage 1/2 PASS、0 findings，再对同一冻结身份同时派2名fresh code-reviewer并行完成Stage 1/2 PASS、0 findings；三票起止身份都必须一致。任何实质修订使全部旧 verdict 失效并从第一名Reviewer 1的Stage 1重启，不得用串行第三票或历史票补足`1+2`。
- [ ] 8.3 对最终审查身份只运行一次重型门禁：`make test`、`make eval`、`make smoke-local`、`make smoke-service`、`make build`、`make license-check`、PostgreSQL 合同、OpenSpec change/all strict、acceptance 和 `git diff --check`；记录命令、退出状态、passed/skipped 与 BLOCKED/NOT RUN 边界。
- [ ] 8.4 在没有两个隔离真实凭据和完整前置时运行零调用 live preflight 并记录 `hosted-unverified`；只有前置完整后的真实外部故障才记录 `external-blocked`，不把任何一者写成 PASS。
- [ ] 8.5 将本文件全部任务勾选，校准最终 changed-file manifest、AC-090～AC-095 映射、审查结论与唯一下一动作；最终 manifest 的模型公共 API 项必须包含 `packages/agent-harness/src/agent_harness/models/__init__.py`，不得把 design 已冻结的导出面留在精确 owner 清单外。终态明确为 `ready-to-archive`，不 sync/archive/commit/push/release/deploy，不创建 Phase 19 change。
