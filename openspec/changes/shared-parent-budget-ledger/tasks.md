## 1. 红灯合同与迁移边界

- [x] 1.1 增加 direct→direct、direct→delegation、delegation→direct 与混合真并发红灯 contracts，分别覆盖 token、cost、双维同时竞争和 `max_cost_usd_per_run=null`，断言 SQLite/PostgreSQL 只提交不越界组合且拒绝路径 provider/child/queue 为零；证明 cost-disabled 时普通 model、embedding miss、delegated child 的合法 null/unavailable cost 不产生 impact/needs_review但非法 cost/status仍拒绝，token维度正常settle/recover/terminal；证明同 tenant 两个 `parent_run_id=null` root 的 `budget_owner_run_id` 分别取自身 `run_id`、ledger 完全隔离，同一 root 的 direct/delegation/allocation 命中同一 owner
- [ ] 1.2 增加model、embedding miss/hit、fallback与无可信有限上界红灯contracts，固定input+output token计量、embedding input计量、受信最坏情况reservation和封闭内部rejection evidence；逐字段证明direct、delegation top-level与allocation各自stable key和版本化immutable identity分离、tenant-scoped keyed request fingerprint只持久化opaque digest/key version、同key同identity exact replay。Direct/allocation覆盖request/usage kind/actual route/tree或target sub-snapshot/price/trusted bound冲突；top-level delegation同时覆盖`0015` request hash与`0016` identity双重验证、target route/price catalog digest、trusted reservation bounds及同hash异identity冲突，所有冲突均在owner/relation mutation、子额度、parent余额/capacity前拒绝。Allocation向parent只映射`delegation.execution_failed`，top-level公开映射`delegation.idempotency_conflict`，unique-race重读同样收敛；其余cache、direct rejection与零副作用边界保持原合同
- [x] 1.2A 增加 soft review threshold 与 hard limit 分层红灯 contracts，固定 `route/context degrade → trusted finite intent → static hard eligibility → soft policy/fallback/approval → current-balance atomic reservation → external side effect`；覆盖无 finite bound 直接拒绝，trusted finite intent 静态超过 frozen hard limit 时在 soft policy/approval 前 hard reject 且 approval/provider/child/queue 全为零，fallback 每次回到 route/trusted-intent 后重新执行 finite-bound 与 hard-eligibility 检查并有限终止，`require_approval` 不持有额度，等待期间余额变化后 approved continuation hard reject，审批不能提高、重置或覆盖 hard limit
- [x] 1.2B 增加root run tree budget snapshot红灯contracts：创建前冻结owner hard limits、cost-enabled状态、registry/config/catalog versions，以及root和当时显式允许targets各自独立descriptor/model-policy/target-budget/route/price sub-snapshot；证明跨agent child继承同一owner snapshot ID/hard limits但使用自身target sub-snapshot，target ceiling只收紧已启用owner维度，source/target descriptor不同合法，target reload只影响新root，未冻结target/route拒绝，fallback只在对应frozen sub-snapshot内重算reservation且不修改limit
- [x] 1.3 增加三个crash window红灯contracts：`side_effect_state=not_started`、`started`但无原子result+settlement、result+settlement已提交而event待补投；证明新writer不产生result-only/ledger-only/cache单边状态，第二阶段needs-review且不重放，第三阶段只补event；同时覆盖unknown/非法/actual-over、terminal fencing、同`usage_call_id`重放与child不双计
- [x] 1.4 增加`0016` fresh upgrade、legacy tree分类、可证明backfill、矛盾状态整批拒绝、SQLite/真实PostgreSQL一致与evidence-aware downgrade contracts；证明无immutable snapshot但root terminal、PostgreSQL唯一terminal row或SQLite terminal status+`terminal_reservation=0`+outstanding=0 closure proof、`0014` outbox与`0015` delegation及queue/approval/recovery全部封闭的tree作为`legacy_closed`保留旧历史且不建ledger，合法旧库可在旧writer drain/reconcile后升级；未封闭且无snapshot的tree必须DDL前拒绝。对需backfill tree固定root-null→自身`run_id`、child-parent+唯一delegation relation→同一非空owner，逐项覆盖settled direct、legacy child allocation、`0015`四态、actual-over与完整immutable descriptor/config/route/price snapshot；current-config-only、hash/version不符、多源冲突、price缺失、child冲突均整批拒绝且两数据库一致
- [x] 1.5 增加application UoW原子组合红灯contracts：direct的`0016 claim + 0014 usage outbox/event capacity`、delegation的`0016 claim + 0015 relation/reservation + 0014 ordered evidence/event capacity`以及可信result persistence + shared settlement必须全部提交或回滚；逐项注入budget、capacity、relation、snapshot、unique、commit失败，证明无半提交、无重复provider/child/queue。逐值断言优先级：exact replay/identity conflict → authorization/owner/relation/snapshot → `event.sequence_state_invalid` → budget → `event.sequence_exhausted` → unique-race重读；budget+capacity返回budget code，capacity-only固定`event.sequence_exhausted`

## 2. Shared Ledger 与 0016

- [ ] 2.1 实现`0016` parent budget ledger、top-level operation claim与delegation child allocation typed models/constraints/唯一关联；以非空`(tenant_id,budget_owner_run_id)`唯一标识并tenant-fenced引用root`AgentRun.run_id`，所有claim/allocation持有同一owner FK，禁止使用nullable`AgentRun.parent_run_id`作为ledger key；冻结owner hard token/cost limits、registry/config/catalog versions、root/允许target各自sub-snapshot与显式cost-disabled状态；为direct、delegation top-level与allocation持久化对应版本化immutable identity hash、opaque fingerprint、schema/key version和非敏感refs，数据库constraint拒绝三类identity空值或ownership形状不匹配，stable key只定位row、identity决定replay/conflict，不新增公开DTO字段
- [x] 2.2 实现统一 repository/UoW：同一 budget owner root/ledger row lock/CAS 下 claim、reserve、settle、release、needs_review、aggregate/detail version 校验和 terminal guard；direct统一返回`budget.reservation_rejected`并仅在脱敏内部evidence保留封闭reason，delegation继续返回`delegation.budget_exceeded`；SQLite 与 PostgreSQL 逐值一致
- [ ] 2.3 实现writers/workers停止窗口的全拓扑分类、独立source backfill与downgrade preflight；严格`legacy_closed` tree只保留旧历史，其余tree只从与bundle分离的durable immutable source和versioned history回填完整snapshot及direct/delegation top-level/allocation三类identity。Top-level delegation必须同时验证`0015` request hash、同canonical request keyed fingerprint、target sub-snapshot/route-price catalog digest和trusted bounds；缺失任一identity、source、price或关系证据均在DDL/UPDATE前fail closed，有evidence时不得删除或释放预算事实

## 3. Direct Model 与 Embedding 接入

- [x] 3.1 让 model router/provider adapter 从当前agent的frozen sub-snapshot为实际 route 产生可信有限 token/cost 最坏上界，并在claim前生成版本化immutable identity；fallback 在封闭route列表内重新形成actual route/trusted intent/identity后claim，无法证明已启用维度上界或target/route未冻结时零 provider side effect 拒绝
- [x] 3.2 让tenant-fenced纯读embedding cache lookup先于reservation；root/direct hit的settled/zero-impact direct claim或delegated child hit的settled/zero-impact allocation，必须与usage result/outbox及event-capacity结算同UoW提交且不建错误ownership；提交前可重做纯读lookup，提交后只补event，不允许单边记录或provider调用；cache miss在provider call前按ownership预约
- [x] 3.3 把`0016` direct claim与`0014` usage outbox/event-capacity reservation接入同一application UoW，并把provider result persistence与shared-budget settlement按稳定`usage_call_id`原子提交；按已启用维度用可信实际值替换reservation，确定零副作用才释放，cache hit走settled zero-impact路径；cost-disabled合法null/unavailable只结算启用维度，非法或unknown/超reservation保守拒绝或needs_review

## 4. Delegation Allocation 与恢复

- [ ] 4.1 让全新 delegation claim 扣除 active direct reservation，并在同一application UoW原子提交`0016` top-level claim、`0015` relation/reservation与`0014` ordered evidence/event capacity；与direct使用同一非空owner。先以`0015` idempotency key/request hash定位relation，再生成并验证包含keyed fingerprint、owner/target snapshot、target route-price catalog digest与trusted bounds的`0016` top-level immutable identity；两层都一致才exact replay，同hash异identity在任何mutation/副作用前映射`delegation.idempotency_conflict`，任一检查失败整组回滚
- [ ] 4.2 让child model/embedding operation在delegation reservation内按已启用维度取得唯一allocation；明确预算语义`delegation_claim_id`逐值等于`AgentDelegation.id`且物理列统一为`delegation_id`，不得生成第二套标识；以`(tenant,owner,delegation_claim_id,usage_call_id)`为stable key并完整实现与direct同构的版本化immutable identity/fingerprint/canonical hash/replay-conflict优先级，新reservation累计不越过子额度；child embedding cache hit建立稳定settled/zero-impact allocation并作为budget aggregate已知0，禁止建立parent direct claim；cost-disabled的合法null/unavailable不创建cost impact或unknown fencing，非法组合仍拒绝；active/已启用维度unknown保留顶层reservation，全部child/terminal在已启用维度可信settled-under时顶层impact=actual并归还差额，单child或多child actual超界时同一parent UoW按保守max提升顶层impact并传播needs_review；parent aggregate只应用顶层差额且不重复direct charge
- [x] 4.3 更新local/service worker recovery：区分reservation且effect未开始、effect已开始但结果未知、result+settlement已提交而event待补投三个crash window；第二阶段needs-review且不重放，第三阶段只补outbox event，复用claim/allocation且不重复provider、child、queue、budget/event reservation

## 5. Composition 与验收证据

- [x] 5.1 在local app、service API、runtime、worker与terminal guard注入同一shared-budget repository/UoW，逐值复用组合检查/错误优先级与三阶段`side_effect_state`恢复；保持OpenAPI无budget ledger route，公开错误和evidence全链路脱敏
- [ ] 5.2 运行定向 unit/contract/integration/eval/local smoke，记录双向顺序、混合并发、direct/delegation top-level/allocation identity replay/conflict、tree/target sub-snapshot与reload、cache/fallback、崩溃恢复、unknown、child 不双计、terminal fencing 与 migration 结果
- [ ] 5.3 运行真实 PostgreSQL/Redis service smoke，证明 row lock/CAS、delegation request-hash/identity双重replay、delivery/reclaim、零重复外部调用、token/cost 不超支和 SQLite/local 逐值一致；不得用 local smoke 替代
- [ ] 5.4 运行 Phase 13.8A 完整质量、全仓测试、build、license、pre-commit、strict validation 与 diff check，并把本 change 的 tasks、实现、验证和 review finding 处置同步到 `ready-to-archive`；Product/API/DEV 与相关 active change 已按确认的新设计方向更新，canonical specs 只在显式 archive 时合并；不 push、不发布或部署

## 6. 归档审查修正

- [ ] 6.1 将 fingerprint key 改为 `BudgetSettings` typed secret，并以 `AGENT_HARNESS_BUDGET__FINGERPRINT_KEY` / `_FILE` 复用 CFG-001；删除 runtime 对 env/path 的旁路读取，补四启动入口、wheel-only、Compose、内容语义与全链路脱敏 contracts
- [ ] 6.2 在 `0016` DDL/UPDATE 前验证完整 parent graph，覆盖三层、孤儿、循环、跨tenant、relation缺失/重复；SQLite与真实PostgreSQL均证明坏节点不能逃过root/direct-child分类
- [ ] 6.3 要求 backfill bundle 引用不同记录ID的 durable immutable source checkpoint/evidence，并交叉验证source、bundle与versioned history；拒绝self-contained bundle自证、current-config回填及缺失/冲突source
- [ ] 6.4 在cost-enabled snapshot中要求model input/output与embedding input必需price非null、非bool、非负且有限，补null key与非法数值的SQLite/PostgreSQL反例
- [ ] 6.5 修复model/embedding/delegation application UoW错误优先级，特别证明embedding的`event.sequence_state_invalid`先于budget，且拒绝路径无provider/claim/outbox/capacity副作用
- [ ] 6.6 为delegation top-level claim补齐stable key、版本化identity、keyed fingerprint、target sub-snapshot/route-price catalog digest、trusted bounds、`0015` request hash双重replay与legacy backfill；同hash异identity必须在所有mutation/副作用前拒绝
- [ ] 6.7 与`agent-delegation-execution`联合验证RUN-002最终投影只保留`RunDetailResponse`；完成全部修复与完整门禁后，先由第1位fresh reviewer审同一冻结范围，PASS后再并行派第2、3位，合并三者Stage 1/2结论
